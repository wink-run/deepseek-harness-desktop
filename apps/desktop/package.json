{
  "name": "@deepseek-ai/dsh-desktop",
  "description": "Electron shell that boots dsh web on loopback and opens a native window",
  "version": "0.1.0",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wink-run/deepseek-harness-desktop.git",
    "directory": "apps/desktop"
  },
  "type": "module",
  "main": "lib/main.js",
  "bin": {
    "dsh-desktop": "lib/bin.js"
  },
  "files": [
    "lib/*.js"
  ],
  "scripts": {
    "build": "tsc -b && tsdown",
    "prepare-resources": "node ./scripts/prepare-resources.mjs",
    "start": "electron .",
    "pack": "pnpm run build && pnpm run prepare-resources && electron-builder --dir",
    "dist": "pnpm run build && pnpm run prepare-resources && electron-builder"
  },
  "license": "MIT",
  "author": "DeepSeek AI",
  "dependencies": {
    "@deepseek-ai/dsh": "workspace:^"
  },
  "devDependencies": {
    "electron": "^36.4.0",
    "electron-builder": "^26.0.12",
    "tsdown": "^0.21.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "build": {
    "appId": "com.deepseek.dsh.desktop",
    "productName": "DeepSeek Harness Desktop",
    "executableName": "DeepSeekHarnessDesktop",
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "directories": {
      "output": "release",
      "buildResources": "build"
    },
    "files": [
      "lib/**/*",
      "package.json",
      "build/icon.png"
    ],
    "extraResources": [
      {
        "from": ".pack/runtime",
        "to": "."
      }
    ],
    "publish": [
      {
        "provider": "github",
        "owner": "wink-run",
        "repo": "deepseek-harness-desktop"
      }
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "icon": "build/icon.icns",
      "target": [
        "dmg",
        "zip"
      ],
      "identity": null
    },
    "win": {
      "icon": "build/icon.png",
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "installerIcon": "build/icon.png",
      "uninstallerIcon": "build/icon.png"
    }
  }
}
